# Set default behavior to automatically normalize line endings.
* text=auto

# Specify Git LFS tracking for large files
*.dll filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text

# Unity-specific entries
Library/PackageCache/com.unity.burst/**/* filter=lfs diff=lfs merge=lfs -text
# Add more Unity-specific file types or directories as needed
